#!/usr/bin/env python

import mailbox
import sys

if len(sys.argv) != 2:
    print(f"Usage: {sys.argv[0]} MBOXPATH")
    sys.exit(1)

mbox = mailbox.mbox(sys.argv[1])
for message in mbox:
    print(str(message.get_payload(decode=True), encoding="utf-8"))
